function solution(arr) {
        return arr.map(x => {
            const X = [];
            for(let i = 0; i < x; i++){
                X.push(x);
            }
            return X;
        }).flat();
}